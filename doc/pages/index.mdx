import { Layout } from "components/landing"
export default Layout

# `ot-builder`

`ot-builder` is a Node.JS library that focuses on font processing. It supports parsing, manipulating and building OpenType fonts.

[→ Get Started](tutorial/intro)