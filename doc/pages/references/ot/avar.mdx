import { Layout } from "templates/api-doc"
import { R, Method, Fn, Member, Decl, Item, NthBit } from "api-doc/tyfmt"
import { number, map, array, Data, Ot, Tag } from "api-doc/exports"
import { optional, tuple } from "api-doc/tycon"
export default Layout

# Namespace <Decl s={Ot.Avar}/>

### Constant <Decl s={Ot.Avar.Tag}/> = `"avar"`

### Type <Decl s={Ot.Avar.SegmentMap}/> = <R s={array(tuple(number,number))}/>

### Class <Decl s={Ot.Avar.Table}/>

#### Constructor

* <Method ctor s={Ot.Avar.Table.constructor} args={{segmentMaps: optional(map(Ot.Var.Dim, Ot.Avar.SegmentMap))}} />

#### Properties

* <Member s={Ot.Avar.Table.segmentMaps} type={map(Ot.Var.Dim, Ot.Avar.SegmentMap)}/>
