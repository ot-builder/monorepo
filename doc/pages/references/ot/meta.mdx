import { Layout } from "templates/api-doc"
import { R, Method, Fn, Member, Decl, Item, NthBit } from "api-doc/tyfmt"
import { number, string, array, Data, Ot, Tag } from "api-doc/exports"
import { optional, either, tuple } from "api-doc/tycon"
export default Layout

# Namespace <Decl s={Ot.Meta} />

### Constant <Decl s={Ot.Meta.Tag} /> = `"meta"`

### Class <Decl s={Ot.Meta.Table}/>

#### Constructor

* <Method ctor s={Ot.Meta.Table.constructor} args={{data:optional(array(tuple(Tag, either(string, "Buffer"))))}}/>

#### Properties

* <Member s={Ot.Meta.Table.data} type={array(tuple(Tag, either(string, "Buffer")))} />
