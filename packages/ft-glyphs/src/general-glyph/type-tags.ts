export namespace GeometryType {
    export const ContourSet: unique symbol = Symbol();
    export const TtReference: unique symbol = Symbol();
    export const GeometryList: unique symbol = Symbol();
}

export namespace HintType {
    export const TtInstruction: unique symbol = Symbol();
    export const CffHint: unique symbol = Symbol();
}
